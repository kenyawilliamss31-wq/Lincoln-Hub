// One place for every color in the app.
// Change a value here and it updates on every screen at once.
// This is why you don't hardcode "#14213d" in twelve different files.

export const colors = {
  navy: "#14213d",      // headings, primary text — swap for Lincoln's exact blue
  orange: "#f0801a",    // accents, stripes, dates — swap for Lincoln's exact orange
  bg: "#f4f4f7",        // page background (light grey)
  card: "#ffffff",      // card background (white)
  grey: "#8a8a94",      // secondary text, labels
  line: "#ececf2",      // dividers and thin borders
};
